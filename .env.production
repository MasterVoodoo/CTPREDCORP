# Production Environment Variables
# These variables are baked into the build at compile time by Vite

# API URL - Leave empty to use same domain (recommended)
# The .htaccess will proxy /api/* requests to Node.js backend
# VITE_API_URL=

# If you need to use a different domain or port, uncomment below:
VITE_API_URL=https://ctpred.com.ph

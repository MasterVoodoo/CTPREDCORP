# Frontend Environment Variables
# Copy this file to .env (in the same directory) and update values

# Backend API URL - MUST include /api at the end
VITE_API_URL=http://localhost:5000/api

# IMPORTANT NOTES:
# 1. This file must be in the ROOT directory (same level as package.json)
# 2. After creating/editing .env, restart the dev server: npm run dev
# 3. The URL must include /api at the end
# 4. Backend must be running on port 5000 (check backend/.env PORT=5000)
# 5. Vite only reads .env on startup, not during hot reload
